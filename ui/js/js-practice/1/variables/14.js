//global variable
//it can be accessed from everywhere

const sigma = "Fahim" //global variable

const sigma_boy = () =>{
    console.log("Sigma boy: ", sigma) //accesing the global variable
}

sigma_boy() //calling the function